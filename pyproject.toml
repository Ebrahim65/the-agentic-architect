[project]
name = "agentic-playground"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "openai>=2.44.0",
    "python-dotenv>=1.2.2",
]
